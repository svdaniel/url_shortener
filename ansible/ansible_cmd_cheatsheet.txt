# create ssh key
ssh-keygen -t rsa -b 4096 -C "daniel.svoboda@twisto.com" -N "" -f ~/.ssh/id_rsa

# add master key to remote host
ansible-playbook playbooks/initial_setup-deploy_authorized_key.yml -i inventory -k

# create priviledged user
ansible-playbook playbooks/create_priviledged_user.yml -i inventory








